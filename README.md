# -fetal-health-project
This project shows fetal health classification using machine learning. The dataset has medical features. I used Python with pandas and scikit‑learn for preprocessing, scaling, and training a Random Forest classifier. The model was evaluated with confusion matrix, classification report, and a heatmap. Results were saved to a text file.
